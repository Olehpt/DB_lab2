@model IEnumerable<LabDomain.Model.Subject>

@{
    ViewData["Title"] = "7";
}
<form asp-action="RequestSevenPost" method="post">
    <div class="row">
        <div class="col-md-4">
            <h1>Предметні області, в яких усі публікації мають хоча б один коментар</h1>
            <button type="submit" class="btn btn-primary mt-2">Send</button>
            <hr />

            @if (Model == null || !Model.Any())
            {
                <p>No items</p>
            }
            else
            {
                <h3>Results:</h3>
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>Id</th>
                            <th>Name</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var pub in Model)
                        {
                            <tr>
                                <td>@pub.SubjectId</td>
                                <td>@pub.Name</td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
        </div>
    </div>

</form>

