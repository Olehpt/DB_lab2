@model IEnumerable<LabDomain.Model.Publication>

@{
    ViewData["Title"] = "2";
}
<form asp-action="RequestTwoPost" method="post">
    <div class="row">
        <div class="col-md-4">
            <h1>Публікації з предметної області</h1>
            <div class="form-group">
                <label for="s">Subject:</label>
                <select name="subjectid" class="form-control" asp-items="ViewBag.Subject"></select>
            </div>
            <button type="submit" class="btn btn-primary mt-2">Send</button>
            <hr />

            @if (Model == null)
            {
                <p>No items</p>
            }
            else
            {
                <h3>Results:</h3>
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>Id</th>
                            <th>Title</th>
                            <th>Subject</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var pub in Model)
                        {
                            <tr>
                                <td>@pub.PublicationId</td>
                                <td>@pub.Title</td>
                                <td>@pub.SubjectNavigation.Name</td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
        </div>
    </div>

</form>
